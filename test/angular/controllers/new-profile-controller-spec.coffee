# describe 'NewProfileController', ->
#   beforeEach ->
#     module 'octobluApp'

#     inject ($controller, $rootScope) =>
#       @rootScope = $rootScope
#       @sut = $controller 'NewProfileController',

#   it 'should exist', ->
#     expect(@sut).to.exist
